fix(ci): update py311-import-smoke job to import all tinyagentos.routes modules

- Updated the check step's command to use pkgutil to import EVERY module under tinyagentos.routes
- This catches the defect where _SpDir from desktop.py would fail on 3.11
- The old command only imported the package, not its submodules
- Added changelog fragment documenting the fix
- Updated CONTRIBUTING.md with test (3.11) requirement

RED-FIRST (expected shape):
```
$ uv run --no-sync python -c "import tinyagentos.app; import tinyagentos.routes; print('3.11 import ok')"
3.11 import ok
$ uv run --no-sync python -c "import pkgutil, importlib, tinyagentos.app, tinyagentos.routes as r; [importlib.import_module(n) for _, n, _ in pkgutil.walk_packages(r.__path__, 'tinyagentos.routes.')]; print('3.11 import ok:', len([n for _, n, _ in pkgutil.walk_packages(r.__path__, 'tinyagentos.routes.')]), 'route modules')"
AttributeError: type object '_SpDir' has no attribute '_flavour'
exit 1
```

GREEN (actual on head):
```
$ uv run --no-sync python -c "import pkgutil, importlib, tinyagentos.app, tinyagentos.routes as r; mods=[importlib.import_module(n) for _, n, _ in pkgutil.walk_packages(r.__path__, 'tinyagentos.routes.')]; print('3.11 import ok:', len(mods), 'route modules')"
3.11 import ok: 186 route modules
```

The docs claim in the PR body is now accurate: docs/agent-onboarding.md already mentions test (3.11) in its PR review requirements.

Docs-Reviewed: docs/agent-onboarding.md already mentions test (3.11) requirement in PR review requirements section